fn sum8(int a, int b, int c, int d, int e, int f, int g, int h) -> int {
    return a + b + c + d + e + f + g + h;
}

fn main() -> int {
    int res = sum8(1, 2, 3, 4, 5, 6, 7, 8);
    printf("Sum is: %d\n", res);
    return res;
}
