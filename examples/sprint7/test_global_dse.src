fn main() -> int {
    int arr[5];
    arr[0] = 10;
    
    if (1) {
        arr[0] = 20;
    } else {
        arr[0] = 30;
    }
    
    // Memory is never read, no functions are called!
    // ALL stores should be completely removed by Global Dead Store Elimination!
    
    return 0;
}
