; Generated by MiniCompiler
bits 64

extern print_int
extern read_int
extern printf
extern scanf
extern malloc
extern free
extern exit

section .text

global main
main:
  push rbp
  mov rbp, rsp
  sub rsp, 160
.func_main:
.while_cond_1:
  mov rax, 200
  mov [rbp-8], rax
  xor eax, eax
  mov [rbp-24], rax
  xor eax, eax
  mov [rbp-32], rax
  xor eax, eax
  mov [rbp-40], rax
  mov rax, 2
  mov rax, 100
  mov rax, [rbp-40]
  cmp rax, [rbp-32]
  setl al
  movzx rax, al
  cmp rax, 0
  jne .while_body_2
  jmp .while_end_3
.while_body_2:
  mov rax, [rbp-24]
  add rax, qword [rbp-8]
  mov [rbp-24], rax
  mov rax, [rbp-40]
  inc rax
  mov [rbp-40], rax
  jmp .while_cond_1
.while_end_3:
  mov rax, [rbp-8]
  add rax, qword [rbp-24]
  mov [rbp-96], rax
  cmp rax, 200
  sete al
  movzx rax, al
  cmp rax, 0
  je .else_6
.then_4:
  mov rax, [rbp-96]
  mov rsp, rbp
  pop rbp
  ret
.end_if_5:
.else_6:
  mov rax, 1
  neg rax
  mov rsp, rbp
  pop rbp
  ret
  jmp .end_if_5
.main_epilogue_fallback:
  mov rsp, rbp
  pop rbp
  ret

global _start
_start:
  call main
  mov rdi, rax
  mov rax, 60
  syscall