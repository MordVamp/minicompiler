; Generated by MiniCompiler
bits 64

extern print_int
extern read_int
extern printf
extern scanf
extern malloc
extern free
extern exit

section .text

global main
main:
  push rbp
  mov rbp, rsp
  sub rsp, 48
  push rbx
  push r12
  push r13
  push r14
  push r15
.func_main:
.while_cond_1:
  mov rax, [rbp-16]
  cmp rax, [rbp-8]
  setl al
  movzx rax, al
  mov r15, rax
  mov rax, r15
  cmp rax, 0
  jne .while_body_2
  jmp .while_end_3
.while_body_2:
  mov rax, r13
  add rax, rbx
  mov r15, rax
  mov r13, r15
  mov rax, [rbp-16]
  inc rax
  mov r15, rax
  mov [rbp-16], r15
  jmp .while_cond_1
.while_end_3:
  mov rax, rbx
  add rax, r13
  mov r15, rax
  mov r14, r15
  mov rax, r14
  cmp rax, 200
  sete al
  movzx rax, al
  mov r15, rax
  mov rax, r15
  cmp rax, 0
  je .else_6
.else_6:
  mov rax, 1
  neg rax
  mov r15, rax
  mov rax, r15
  pop r15
  pop r14
  pop r13
  pop r12
  pop rbx
  mov rsp, rbp
  pop rbp
  ret
  jmp .end_if_5
.then_4:
  mov rax, r14
  pop r15
  pop r14
  pop r13
  pop r12
  pop rbx
  mov rsp, rbp
  pop rbp
  ret
.end_if_5:
.main_epilogue_fallback:
  pop r15
  pop r14
  pop r13
  pop r12
  pop rbx
  mov rsp, rbp
  pop rbp
  ret

global _start
_start:
  call main
  mov rdi, rax
  mov rax, 60
  syscall