; Generated by MiniCompiler
bits 64

extern print_int
extern read_int
extern printf
extern scanf
extern malloc
extern free
extern exit

section .text

global main
main:
  push rbp
  mov rbp, rsp
  sub rsp, 16
  push rbx
  push r12
  push r13
  push r14
  push r15
.func_main:
.while_cond_1:
  mov rax, r13
  cmp rax, r12
  setle al
  movzx rax, al
  mov r14, rax
  mov rax, r14
  cmp rax, 0
  jne .while_body_2
  jmp .while_end_3
.while_body_2:
  mov rax, r13
  cqo
  mov rcx, 2
  idiv rcx
  mov r15, rdx
  mov rax, r15
  cmp rax, 0
  sete al
  movzx rax, al
  mov r14, rax
  mov rax, r14
  cmp rax, 0
  je .else_6
.else_6:
.end_if_5:
  mov rax, r13
  inc rax
  mov r14, rax
  mov r13, r14
  jmp .while_cond_1
.while_end_3:
  mov rax, rbx
  pop r15
  pop r14
  pop r13
  pop r12
  pop rbx
  mov rsp, rbp
  pop rbp
  ret
.main_epilogue_fallback:
  pop r15
  pop r14
  pop r13
  pop r12
  pop rbx
  mov rsp, rbp
  pop rbp
  ret

global _start
_start:
  call main
  mov rdi, rax
  mov rax, 60
  syscall